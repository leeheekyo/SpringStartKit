<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.naver.blog.point.dao.PointChargeDao">
    
    <select id="sampleData" resultType="java.math.BigInteger">
   		SELECT COUNT(1)
		  FROM DUAL
    </select>
    
</mapper>